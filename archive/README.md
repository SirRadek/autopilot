# Archive

This directory keeps legacy project trees that are preserved for audit and selective migration.

## `autopilot-control-plane-v0.2.0/`

Snapshot of the former `SirRadek/autopilot` `main` branch at commit `93f3d46`.

It is intentionally archived under a prefix instead of being restored into the repository root because the current root is the `autopilot-clientops-cms` v0.1 Next.js/Payload application.

Archived code is not part of the active build, lint, typecheck, or runtime surface. Use it only as source material when migrating useful control-plane pieces into the active ClientOps/Decision Mesh structure.
